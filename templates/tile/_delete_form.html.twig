<form method="post" action="{{ path('app_tile_delete', {'id': tile.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ tile.id) }}">
    <button class="btn">Delete</button>
</form>
