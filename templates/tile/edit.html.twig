<!DOCTYPE html>

<title>Edit Tile</title>

{% block body %}
    <h1>Edit Tile</h1>

    {{ include('tile/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('app_tile_index') }}">back to list</a>

    {{ include('tile/_delete_form.html.twig') }}
{% endblock %}
