<!DOCTYPE html>

<title>New Tile</title>

{% block body %}
    <h1>Create new Tile</h1>

    {{ include('tile/_form.html.twig') }}

    <a href="{{ path('app_tile_index') }}">back to list</a>
{% endblock %}
