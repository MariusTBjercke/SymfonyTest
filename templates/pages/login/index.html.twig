{% extends "grid/base.html.twig" %}
{% set css = "login" %}

{% block title %}{{ 'user.new'|trans }}{% endblock %}

{% block maincontent %}

    <div class="container">
        <div class="row">
            <div class="col">
                {% for message in app.flashes('error') %}
                    <div class="alert alert-danger">
                        {{ message }}
                    </div>
                {% endfor %}

                <h1>{{ 'login.title'|trans }}</h1>

                {{ include('pages/login/_form.html.twig') }}
            </div>
        </div>
    </div>

{% endblock %}
