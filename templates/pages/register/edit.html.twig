<!DOCTYPE html>

<title>Edit User</title>

{% block body %}
    <h1>Edit User</h1>

    {{ include('pages/register/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('user_index') }}">back to list</a>

    {{ include('pages/register/_delete_form.html.twig') }}
{% endblock %}
